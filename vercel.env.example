# Copy this file and fill in your values, then import to Vercel
# Or copy-paste the variables below into Vercel's env form

# Required - Get from Neon dashboard
DATABASE_URL=postgresql://user:password@ep-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require

# Required - Your Vercel app URL (update after first deploy)
NEXTAUTH_URL=https://your-app.vercel.app

# Required - Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-32-char-secret-here

# Leave empty for same-origin (API runs on Vercel)
NEXT_PUBLIC_API_URL=

# Optional - Housing search (get key at https://app.rentcast.io/app/api)
RENTCAST_API_KEY=

# Optional - OAuth (add if using sign-in)
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=
